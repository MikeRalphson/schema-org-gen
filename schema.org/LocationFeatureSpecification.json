{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/LocationFeatureSpecification.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Location Feature Specification",
  "description": "Specifies a location feature by providing a structured value representing a feature of an accommodation as a property-value pair of varying degrees of formality.",
  "media": {
    "type": "application/json;profile=https://schema.org/LocationFeatureSpecification"
  },
  "allOf": [
    {
      "$ref": "PropertyValue.json"
    }
  ],
  "type": "object",
  "properties": {
    "hoursAvailable": {
      "$ref": "OpeningHoursSpecification.json#/$defs/possibleRef"
    },
    "validFrom": {
      "$ref": "#/$defs/validFrom"
    },
    "validThrough": {
      "$ref": "#/$defs/validThrough"
    }
  },
  "$defs": {
    "arrayOfLocationFeatureSpecifications": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "validFrom": {
      "title": "Valid From",
      "description": "The date when the item becomes valid.",
      "type": "string",
      "format": "date-time"
    },
    "validThrough": {
      "title": "Valid Through",
      "description": "The date after when the item is not valid. For example the end of an offer, salary period, or a period of opening hours.",
      "type": "string",
      "format": "date-time"
    }
  },
  "enum": [
    "amenityFeature"
  ]
}