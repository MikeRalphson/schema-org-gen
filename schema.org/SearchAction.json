{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/SearchAction.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Search Action",
  "description": "The act of searching for an object.\n\nRelated actions:\n\n\nFindAction: SearchAction generally leads to a FindAction, but not necessarily.",
  "media": {
    "type": "application/json;profile=https://schema.org/SearchAction"
  },
  "allOf": [
    {
      "$ref": "Action.json"
    }
  ],
  "type": "object",
  "properties": {
    "query": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/query"
      }
    }
  },
  "$defs": {
    "arrayOfSearchActions": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "query": {
      "title": "Query",
      "description": "A sub property of instrument. The query used on this action.",
      "type": "string"
    }
  }
}