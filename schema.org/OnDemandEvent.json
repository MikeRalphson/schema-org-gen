{
  "$schema": "https://TODO",
  "$id": "https://schema.org/OnDemandEvent.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "On Demand Event",
  "description": "A publication event e.g. catch-up TV or radio podcast, during which a program is available on-demand.",
  "media": {
    "type": "application/json;profile=http://schema.org/OnDemandEvent"
  },
  "allOf": [
    {
      "$ref": "PublicationEvent.json"
    }
  ],
  "$defs": {
    "arrayOfOnDemandEvents": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    }
  }
}