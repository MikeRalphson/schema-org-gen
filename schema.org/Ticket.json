{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/Ticket.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Ticket",
  "description": "Used to describe a ticket to an event, a flight, a bus ride, etc.",
  "media": {
    "type": "application/json;profile=https://schema.org/Ticket"
  },
  "allOf": [
    {
      "$ref": "Intangible.json"
    }
  ],
  "type": "object",
  "properties": {
    "dateIssued": {
      "$ref": "#/$defs/dateIssued"
    },
    "issuedBy": {
      "$ref": "Organization.json#/$defs/possibleRef"
    },
    "priceCurrency": {
      "$ref": "#/$defs/priceCurrency"
    },
    "ticketNumber": {
      "$ref": "#/$defs/ticketNumber"
    },
    "ticketToken": {
      "oneOf": [
        {
          "$ref": "#/$defs/ticketToken"
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/ticketToken"
          }
        }
      ]
    },
    "ticketedSeat": {
      "$ref": "Seat.json#/$defs/possibleRef"
    },
    "totalPrice": {
      "$ref": "#/$defs/totalPrice"
    },
    "underName": {
      "$ref": "#/$defs/underName"
    }
  },
  "$defs": {
    "arrayOfTickets": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "dateIssued": {
      "title": "Date Issued",
      "description": "The date the ticket was issued.",
      "type": "string",
      "format": "date-time"
    },
    "priceCurrency": {
      "title": "Price Currency",
      "description": "The currency (in 3-letter ISO 4217 format) of the price or a price component, when attached to PriceSpecification and its subtypes.",
      "type": "string"
    },
    "ticketNumber": {
      "title": "Ticket Number",
      "description": "The unique identifier for the ticket.",
      "type": "string"
    },
    "ticketToken": {
      "title": "Ticket Token",
      "description": "Reference to an asset (e.g., Barcode, QR code image or PDF) usable for entrance.",
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "string",
          "format": "uri"
        }
      ]
    },
    "totalPrice": {
      "title": "Total Price",
      "description": "The total price for the reservation or ticket, including applicable taxes, shipping, etc.",
      "anyOf": [
        {
          "type": "string"
        },
        {
          "$ref": "PriceSpecification.json#/$defs/possibleRef"
        },
        {
          "type": "number"
        }
      ]
    },
    "underName": {
      "title": "Under Name",
      "description": "The person or organization the reservation or ticket is for.",
      "anyOf": [
        {
          "$ref": "Organization.json#/$defs/possibleRef"
        },
        {
          "$ref": "Person.json#/$defs/possibleRef"
        }
      ]
    }
  },
  "enum": [
    "reservedTicket"
  ]
}