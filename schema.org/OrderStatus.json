{
  "$schema": "https://TODO",
  "$id": "https://schema.org/OrderStatus.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Order Status",
  "description": "Enumerated status values for Order.",
  "media": {
    "type": "application/json;profile=http://schema.org/OrderStatus"
  },
  "allOf": [
    {
      "$ref": "Enumeration.json"
    }
  ],
  "$defs": {
    "arrayOfOrderStatuss": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    }
  },
  "enum": [
    "orderItemStatus",
    "orderStatus"
  ]
}