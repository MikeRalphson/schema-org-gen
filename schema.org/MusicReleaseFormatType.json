{
  "$schema": "https://TODO",
  "$id": "https://schema.org/MusicReleaseFormatType.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Music Release Format Type",
  "description": "Format of this release (the type of recording media used, ie. compact disc, digital media, LP, etc.).",
  "media": {
    "type": "application/json;profile=http://schema.org/MusicReleaseFormatType"
  },
  "allOf": [
    {
      "$ref": "Enumeration.json"
    }
  ],
  "$defs": {
    "arrayOfMusicReleaseFormatTypes": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    }
  },
  "enum": [
    "musicReleaseFormat"
  ]
}