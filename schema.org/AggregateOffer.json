{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/AggregateOffer.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Aggregate Offer",
  "description": "When a single product is associated with multiple offers (for example, the same pair of shoes is offered by different merchants), then AggregateOffer can be used.",
  "media": {
    "type": "application/json;profile=https://schema.org/AggregateOffer"
  },
  "allOf": [
    {
      "$ref": "Offer.json"
    }
  ],
  "type": "object",
  "properties": {
    "highPrice": {
      "$ref": "#/$defs/highPrice"
    },
    "lowPrice": {
      "$ref": "#/$defs/lowPrice"
    },
    "offerCount": {
      "$ref": "#/$defs/offerCount"
    },
    "offers": {
      "$ref": "Offer.json#/$defs/possibleRefArray"
    }
  },
  "$defs": {
    "arrayOfAggregateOffers": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "highPrice": {
      "title": "High Price",
      "description": "The highest price of all offers available.",
      "anyOf": [
        {
          "type": "number"
        },
        {
          "type": "string"
        }
      ]
    },
    "lowPrice": {
      "title": "Low Price",
      "description": "The lowest price of all offers available.",
      "anyOf": [
        {
          "type": "number"
        },
        {
          "type": "string"
        }
      ]
    },
    "offerCount": {
      "title": "Offer Count",
      "description": "The number of offers for the product.",
      "type": "integer"
    }
  }
}