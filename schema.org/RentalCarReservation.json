{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/RentalCarReservation.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Rental Car Reservation",
  "description": "A reservation for a rental car.\n\nNote: This type is for information about actual reservations, e.g. in confirmation emails or HTML pages with individual confirmations of reservations.",
  "media": {
    "type": "application/json;profile=https://schema.org/RentalCarReservation"
  },
  "allOf": [
    {
      "$ref": "Reservation.json"
    }
  ],
  "type": "object",
  "properties": {
    "dropoffLocation": {
      "oneOf": [
        {
          "$ref": "Place.json#/$defs/possibleRef"
        },
        {
          "type": "array",
          "items": {
            "$ref": "Place.json#/$defs/possibleRef"
          }
        }
      ]
    },
    "dropoffTime": {
      "oneOf": [
        {
          "$ref": "#/$defs/dropoffTime"
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/dropoffTime"
          }
        }
      ]
    },
    "pickupLocation": {
      "oneOf": [
        {
          "$ref": "Place.json#/$defs/possibleRef"
        },
        {
          "type": "array",
          "items": {
            "$ref": "Place.json#/$defs/possibleRef"
          }
        }
      ]
    },
    "pickupTime": {
      "oneOf": [
        {
          "$ref": "#/$defs/pickupTime"
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/pickupTime"
          }
        }
      ]
    }
  },
  "$defs": {
    "arrayOfRentalCarReservations": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "dropoffTime": {
      "title": "Dropoff Time",
      "description": "When a rental car can be dropped off.",
      "type": "string",
      "format": "date-time"
    },
    "pickupTime": {
      "title": "Pickup Time",
      "description": "When a taxi will pickup a passenger or a rental car can be picked up.",
      "type": "string",
      "format": "date-time"
    }
  }
}