{
  "$schema": "https://TODO",
  "$id": "https://schema.org/ReservationStatusType.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Reservation Status Type",
  "description": "Enumerated status values for Reservation.",
  "media": {
    "type": "application/json;profile=http://schema.org/ReservationStatusType"
  },
  "allOf": [
    {
      "$ref": "Enumeration.json"
    }
  ],
  "$defs": {
    "arrayOfReservationStatusTypes": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    }
  },
  "enum": [
    "reservationStatus"
  ]
}