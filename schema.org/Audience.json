{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/Audience.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Audience",
  "description": "Intended audience for an item, i.e. the group for whom the item was created.",
  "media": {
    "type": "application/json;profile=https://schema.org/Audience"
  },
  "allOf": [
    {
      "$ref": "Intangible.json"
    }
  ],
  "type": "object",
  "properties": {
    "audienceType": {
      "$ref": "#/$defs/audienceType"
    },
    "geographicArea": {
      "$ref": "AdministrativeArea.json#/$defs/possibleRef"
    }
  },
  "$defs": {
    "arrayOfAudiences": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "audienceType": {
      "title": "Audience Type",
      "description": "The target group associated with a given audience (e.g. veterans, car owners, musicians, etc.).",
      "type": "string"
    }
  },
  "enum": [
    "audience",
    "grantee",
    "permitAudience",
    "recipient",
    "sender",
    "toRecipient",
    "touristType"
  ]
}