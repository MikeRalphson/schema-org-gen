{
  "$schema": "https://TODO",
  "$id": "https://schema.org/ReservationPackage.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Reservation Package",
  "description": "A group of multiple reservations with common values for all sub-reservations.",
  "media": {
    "type": "application/json;profile=http://schema.org/ReservationPackage"
  },
  "allOf": [
    {
      "$ref": "Reservation.json"
    }
  ],
  "type": "object",
  "properties": {
    "subReservation": {
      "$ref": "Reservation.json#/$defs/possibleRef"
    }
  },
  "$defs": {
    "arrayOfReservationPackages": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    }
  }
}