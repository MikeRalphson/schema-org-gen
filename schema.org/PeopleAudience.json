{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/PeopleAudience.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "People Audience",
  "description": "A set of characteristics belonging to people, e.g. who compose an item's target audience.",
  "media": {
    "type": "application/json;profile=https://schema.org/PeopleAudience"
  },
  "allOf": [
    {
      "$ref": "Audience.json"
    }
  ],
  "type": "object",
  "properties": {
    "healthCondition": {
      "$ref": "MedicalCondition.json#/$defs/possibleRef"
    },
    "requiredGender": {
      "$ref": "#/$defs/requiredGender"
    },
    "requiredMaxAge": {
      "$ref": "#/$defs/requiredMaxAge"
    },
    "requiredMinAge": {
      "$ref": "#/$defs/requiredMinAge"
    },
    "suggestedGender": {
      "$ref": "#/$defs/suggestedGender"
    },
    "suggestedMaxAge": {
      "$ref": "#/$defs/suggestedMaxAge"
    },
    "suggestedMinAge": {
      "$ref": "#/$defs/suggestedMinAge"
    }
  },
  "$defs": {
    "arrayOfPeopleAudiences": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "requiredGender": {
      "title": "Required Gender",
      "description": "Audiences defined by a person's gender.",
      "type": "string"
    },
    "requiredMaxAge": {
      "title": "Required Max Age",
      "description": "Audiences defined by a person's maximum age.",
      "type": "integer"
    },
    "requiredMinAge": {
      "title": "Required Min Age",
      "description": "Audiences defined by a person's minimum age.",
      "type": "integer"
    },
    "suggestedGender": {
      "title": "Suggested Gender",
      "description": "The gender of the person or audience.",
      "type": "string"
    },
    "suggestedMaxAge": {
      "title": "Suggested Max Age",
      "description": "Maximal age recommended for viewing content.",
      "type": "number"
    },
    "suggestedMinAge": {
      "title": "Suggested Min Age",
      "description": "Minimal age recommended for viewing content.",
      "type": "number"
    }
  }
}