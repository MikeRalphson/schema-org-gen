{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.org/FlightReservation.json",
  "$comment": "Autogenerated with schema-org-gen",
  "title": "Flight Reservation",
  "description": "A reservation for air travel.\n\nNote: This type is for information about actual reservations, e.g. in confirmation emails or HTML pages with individual confirmations of reservations. For offers of tickets, use Offer.",
  "media": {
    "type": "application/json;profile=https://schema.org/FlightReservation"
  },
  "allOf": [
    {
      "$ref": "Reservation.json"
    }
  ],
  "type": "object",
  "properties": {
    "boardingGroup": {
      "$ref": "#/$defs/boardingGroup"
    },
    "passengerPriorityStatus": {
      "$ref": "#/$defs/passengerPriorityStatus"
    },
    "passengerSequenceNumber": {
      "$ref": "#/$defs/passengerSequenceNumber"
    },
    "securityScreening": {
      "$ref": "#/$defs/securityScreening"
    }
  },
  "$defs": {
    "arrayOfFlightReservations": {
      "type": "array",
      "items": {
        "$ref": "#"
      }
    },
    "possibleRef": {
      "oneOf": [
        {
          "$ref": "#"
        },
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        }
      ]
    },
    "possibleRefArray": {
      "oneOf": [
        {
          "type": "string",
          "format": "uri",
          "links": [
            {
              "rel": "full",
              "href": "{+$}"
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/$defs/possibleRef"
          }
        }
      ]
    },
    "boardingGroup": {
      "title": "Boarding Group",
      "description": "The airline-specific indicator of boarding order / preference.",
      "type": "string"
    },
    "passengerPriorityStatus": {
      "title": "Passenger Priority Status",
      "description": "The priority status assigned to a passenger for security or boarding (e.g. FastTrack or Priority).",
      "anyOf": [
        {
          "$ref": "QualitativeValue.json#/$defs/possibleRef"
        },
        {
          "type": "string"
        }
      ]
    },
    "passengerSequenceNumber": {
      "title": "Passenger Sequence Number",
      "description": "The passenger's sequence number as assigned by the airline.",
      "type": "string"
    },
    "securityScreening": {
      "title": "Security Screening",
      "description": "The type of security screening the passenger is subject to.",
      "type": "string"
    }
  }
}